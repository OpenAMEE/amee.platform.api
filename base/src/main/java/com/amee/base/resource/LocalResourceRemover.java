package com.amee.base.resource;

/**
 * An empty implementation of {@link ResourceRemover} which extends {@link LocalResourceHandler}. This class
 * is useful when configuring resources in the Spring XML config files.
 */
public class LocalResourceRemover extends LocalResourceHandler implements ResourceRemover {
}
